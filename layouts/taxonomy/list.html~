{{ define "main" }}
<h1>#{{ .Title }}</h1>
<ul class="pagelist">
  {{ range .Pages.ByDate }}
  <li>{{ if .IsPage }}{{ dateFormat "Jan 2, 2006" .Date }} &mdash; {{ end }}
        <a href="{{ .RelPermalink }}">{{ .Title }}</a>
        {{ if .IsNode }}
        : {{ len .Page.Pages }}
        {{ end }}
      </li>
  {{ end }}
</ul>
{{ end }}
