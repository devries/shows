{{ if and (.Get "width") (.Get "height") }}
<img class="responsive" src="{{ .Get `src` }}" alt="{{ .Get `alt` }}" width="{{ .Get `width` }}" height="{{ .Get `height` }}"/>
{{ else }}
<img class="responsive" src="{{ .Get `src` }}" alt="{{ .Get `alt` }}"/>
{{ end }}
